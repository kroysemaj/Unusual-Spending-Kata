const determineHighSpending = require('./determine-high-spending');
// const paymentApi = require('./payment-api');

// Arrange
const userId = 42;
const subject = require('./fetch-payments');


xdescribe('fetch the payments', () => {
    it('should pass', () => {
        expect(true).toBe(true)
    });
});